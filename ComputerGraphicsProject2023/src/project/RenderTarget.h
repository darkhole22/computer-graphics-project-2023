#pragma once

#include "vulkan_wrapper.h"

namespace computergraphicsproject {

class RenderTarget
{
public:
	RenderTarget(const SwapChain& swapChain, const Device& divice);

	~RenderTarget();
private:


};

}